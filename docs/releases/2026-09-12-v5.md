# V5 — publicación y portada

**Actualización vigente:** chevrones B en home y subpáginas, publicados el 2026-09-12 a las 20:01:05 UTC. Runtime `8276775`; conserva el header mobile opción 4, las tarjetas C y los mocks de Recetas. [Publicación, verificación y rollback](2026-09-12-chevron-b.md).

Publicada y verificada en <https://alvia.ar/v5/> el 12 de septiembre de 2026 UTC. Posteriormente aprobada por Pablo y promovida a portada de `alvia.ar` y `www.alvia.ar`, según el registro al final de este documento.

Pablo pidió una V5 con el estilo visual, comunicativo y comercial de 1doc3, corrigió el titular y definió tres ejes: ampliar la oferta de turnos/consultas sin ampliar estructura, tecnología al servicio de gestión/atención y valor agregado para afiliados. También pidió aplicar `telemed-starter/docs/COPYWRITING.md`. La versión publicada incorpora esas correcciones.

- Fuente: `v5/` en `Raboys/alvia-web`.
- Rama: `feat/v5-benefit-led`.
- PR: [#20](https://github.com/Raboys/alvia-web/pull/20), inicialmente borrador y luego aprobado e integrado a `main` el 2026-09-12 a las 06:02:22 UTC; merge `0673bc409a0b5b75e7890c20213217a2850d24c9`.
- Commit de runtime publicado: `c2817c16b87c28a2235a03433018047e4e725fbe`.
- Release: `/var/www/alvia.ar/releases/v5-c2817c1`.
- Enlace público: `/var/www/alvia.ar/www/v5` → ese release.
- Manifiesto: <https://alvia.ar/v5/release.json>.
- Paquete: 11 archivos, 423.228 bytes, más manifiesto. Incluye fuentes locales y licencias; excluye documentación y herramientas.
- Conserva `noindex, nofollow` como las versiones de revisión anteriores.

## Verificación

146 comprobaciones locales y 146 sobre la URL pública, en Chrome a 1440, 1024, 768, 390 y 320 px. Sin errores de recursos ni JavaScript, imágenes cargadas y sin desborde horizontal. Se verificaron el titular, anclas, destinos de ventas, calculadora, límites/entradas inválidas, redondeo de turnos por profesional, selector de app, pestañas por clic y teclado, preguntas y navegación móvil.

Sin JavaScript: navegación disponible, contenido de todos los segmentos visible, selector nativo de app funcional y ejemplo estático de capacidad. Con movimiento reducido no se usa scroll suave.

En la primera publicación, los 11 archivos de runtime servidos por Nginx coincidieron byte a byte con el manifiesto. Nginx, V1, V2, V3 y V4 conservaron sus hashes previos; en ese momento la portada todavía redirigía a `/v4/`. La promoción posterior sólo cambia esa redirección, como se detalla abajo.

Evidencia de autoría:

- [Capturas y resultados públicos conservados en Git](../design/v5-baseline/README.md).
- `/tmp/alvia-v5-review/checks.json` y capturas locales.
- `/tmp/alvia-v5-public-review/checks.json` y capturas públicas.
- `/tmp/alvia-v5-before.json`, hashes anteriores a publicar.

## Retirar la versión

Mientras la portada apunte a V5, primero devolver las raíces a V4 mediante el procedimiento de abajo y verificar los accesos. Después, si se necesita retirar también la ruta V5, comprobar que `/var/www/alvia.ar/www/v5` siga apuntando a `/var/www/alvia.ar/releases/v5-c2817c1` y retirar solamente ese enlace. El release se conserva. No se borra ni se sincroniza el document root completo.

La primera publicación fue para revisión. La aprobación posterior incluyó la integración a `main` y la promoción a portada.

## V5 como portada — 2026-09-12 UTC

Pablo aprobó la página publicada y pidió ponerla inmediatamente en `alvia.ar` y `www.alvia.ar`. Ambos accesos ahora redirigen con 301 a `https://alvia.ar/v5/`. V4 sigue accesible por su enlace directo.

Sólo se cambió el destino de `location = /` en el servidor institucional de `/etc/nginx/sites-available/alvia.ar`, junto con sus comentarios. Se validó con `sudo nginx -t` y se aplicó con `sudo systemctl reload nginx`. El runtime servido en V5 permanece en `c2817c1`.

Comprobación de las dos raíces desde origen y Cloudflare: 301 a V5. V5 en apex y www y V4 directa: 200. Evidencia: `/tmp/alvia-home-v5-routing.json`. Capturas de navegador al entrar por apex en escritorio y www en móvil: `/tmp/alvia-home-v5/`.

Backup: `/etc/nginx/sites-available/alvia.ar.before-home-v5-20260912`. Para revertir solamente la portada, devolver el destino de `location = /` a `https://alvia.ar/v4/`, validar Nginx y recargar. No restaurar el archivo completo si hubo otros cambios posteriores.

El PR #20 pasó de borrador a listo y fue integrado a `main` como parte de esta aprobación. El merge contiene el mismo runtime publicado; los commits siguientes a `c2817c1` dentro del PR sólo documentan la publicación y la portada.

## Features en home · publicación para revisión — 2026-09-12 UTC

Pablo pidió publicar la ampliación en V5 para verla. Se publicó «Mucho más que una videollamada», con Validaciones, AI Notes y Recetas después de las modalidades de red. Las tres entradas permanecen informativas y sin enlaces, como indicó; las subpáginas se desarrollarán luego, una por corrida, desde `v5/PLAN_FEATURES.md`.

- URL: <https://alvia.ar/v5/#funcionalidades>.
- Runtime: `0b5a3c10b397fc34626dd0d07b8222b4780d4655`.
- Rama: `feat/v5-feature-pages-plan`; [PR #22](https://github.com/Raboys/alvia-web/pull/22), borrador sin integrar a main.
- Release nuevo: `/var/www/alvia.ar/releases/v5-0b5a3c1`.
- Enlace `/var/www/alvia.ar/www/v5` actualizado atómicamente al nuevo release.
- Release anterior conservado: `/var/www/alvia.ar/releases/v5-c2817c1`.
- Paquete: 12 archivos, 433.097 bytes, más manifiesto; exactamente el contenido del commit validado.
- Nginx, rutas de portada, versiones anteriores y aplicaciones no se modificaron. No se reiniciaron servicios.

Verificación pública: 146 comprobaciones de regresión y 40 del bloque en Chrome, cinco anchos de 320 a 1440 px, sin errores ni desborde; teclado, fallback sin JS y movimiento reducido. SHA-256 de los 12 archivos verificado desde origen. Los 11 recursos coinciden también desde Cloudflare; su HTML agrega únicamente la protección automática de correo y el script para decodificarlo, transformación comprobada y normalizada contra el original. Manifiesto público correcto; ambas raíces mantienen 301 a V5, `www` V5 y V4 responden 200. [Capturas y resultados](../design/v5-features/README.md).

Para revertir sólo esta ampliación: verificar que `www/v5` siga apuntando a `releases/v5-0b5a3c1`; crear un enlace temporal al release anterior `v5-c2817c1` y renombrarlo atómicamente sobre `www/v5`. No cambiar la redirección de las raíces ni eliminar releases. Esta publicación permite revisar la composición; no se interpreta como aprobación final del diseño.

## AI Notes y estudios previos — 2026-09-12 UTC

Pablo pidió publicar AI Notes, documentar su corrección editorial y hacer commit. La versión publicada prioriza documentación automatizada, consultas más ágiles, menos trabajo administrativo y estudios recibidos previamente por email o WhatsApp. La entrada de AI Notes en home ya tiene enlace; Validaciones y Recetas siguen informativas.

- Página: <https://alvia.ar/v5/ai-notes.html>, también disponible en `www.alvia.ar`.
- Runtime: **`a4ca4aa824b49d767349cca3bc78b1004e7702e2`**.
- Rama: `feat/v5-feature-pages-plan`, repositorio `Raboys/alvia-web`.
- Release: `/var/www/alvia.ar/releases/v5-a4ca4aa`.
- Symlink: `/var/www/alvia.ar/www/v5` → ese release; actualizado con rename atómico.
- Release anterior conservado: `/var/www/alvia.ar/releases/v5-0b5a3c1`.
- Paquete: 16 archivos, 451.577 bytes, más `release.json`, generado con `scripts/package_v5.py --ref a4ca4aa`. Coincide exactamente con los hashes del runtime ya revisado; smoke del paquete por HTTP en 1440 y 390 px antes del cambio público.
- El enlace público se actualizó el 2026-09-12 07:11:04 UTC; la verificación posterior terminó correctamente. No se tocó la configuración de Nginx ni se reiniciaron servicios.

**Corrección editorial guardada:** prosa directa y beneficio operativo antes que lemas; menos texto y secciones; sin juegos de palabras, tono poético o condescendiente; no repetir revisión/firma; agrupar capacidades que ayudan a completar la misma tarea. La corrección y las reglas para siguientes features están en [DESIGN](../../v5/DESIGN.md#reglas-para-continuar-las-páginas), [PLAN_FEATURES](../../v5/PLAN_FEATURES.md) y el [brief](../features/ai-notes/BRIEF.md).

**Verificación pública:** 148 comprobaciones de AI Notes y navegación y 146 de home, en cinco anchos de 320 a 1440 px. Sin desborde ni errores de recursos/JS; copia exacta y fallback, teclado, consentimiento, menú, no-JS y movimiento reducido. Se compararon los 16 archivos por SHA-256 desde origen y usando las URLs que el HTML referencia en ambos dominios. El HTML público sólo requiere normalizar la protección de correo de Cloudflare para coincidir. Manifiesto exacto en origen, apex y www. [Resultados, capturas y herramientas reproducibles](../features/ai-notes/README.md#publicación--2026-09-12-utc).

**Estado de rutas encontrado y preservado:** las dos raíces responden 301 hacia `https://alvia.ar/v5/`; V5 responde 200. `/v4/` ya estaba retirada y redirigía a V5 antes de esta publicación, a diferencia del estado histórico de las secciones anteriores. Se conservó su redirección; este despliegue no retiró versiones ni cambió otras rutas. El hash de configuración Nginx permanece igual al preflight.

**Detalle de CDN para próximas publicaciones:** comprobar CSS/JS con las versiones de query presentes en el HTML; una URL sin query puede seguir cacheada con el contenido anterior. El navegador recibe las referencias versionadas correctas. Cloudflare agrega su decodificador de email y puede agregar su beacon de observabilidad; no fueron incorporados por la página. El verificador de navegador permite únicamente esos recursos conocidos además del runtime y sigue rechazando solicitudes inesperadas al producto. No se cambió la configuración de Cloudflare ni se purgó la caché global.

### Revertir sólo AI Notes

Confirmar que `www/v5` siga apuntando a `releases/v5-a4ca4aa`; crear un enlace temporal hacia `releases/v5-0b5a3c1` y renombrarlo atómicamente sobre `www/v5`. Verificar la home y su manifiesto. No modificar redirecciones, restaurar Nginx ni borrar releases. El runtime de esta publicación queda separado del commit posterior de evidencia y documentación.


## Validaciones — 2026-09-12 UTC

Pablo aprobó la página de Validaciones y pidió publicarla, actualizar los Markdown, commitear y pushear. La página reúne identidad, código de credencial y copagos según cobertura. El ejemplo nativo compara una cobertura con copago y otra sin copago; se explican DNI/selfie, código de la app de cobertura y pago al reservar un turno o antes de entrar a la espera de guardia. Se conserva la composición aprobada y el contenido revisado.

- Página: <https://alvia.ar/v5/validaciones.html>, también disponible en <https://www.alvia.ar/v5/validaciones.html>.
- Runtime: **`60dadfc3983dc631609ec2f778a21a942f65ef2a`**. Rama `feat/v5-feature-pages-plan`, repositorio `Raboys/alvia-web`.
- Release: `/var/www/alvia.ar/releases/v5-60dadfc`. Enlace público `/var/www/alvia.ar/www/v5`, actualizado atómicamente el **2026-09-12 17:43:31 UTC**.
- Release anterior conservado: `/var/www/alvia.ar/releases/v5-a4ca4aa`.
- Paquete generado con `python3 scripts/package_v5.py --ref 60dadfc`: 18 archivos, 472.984 bytes, más manifiesto. Los hashes son idénticos a la preview aprobada. Antes del cambio público pasó 205 comprobaciones por HTTP desde el paquete de commit.
- Home enlaza Validaciones y AI Notes; ambas páginas incluyen enlaces recíprocos. Recetas sigue sin destino. El bloque de home conserva su peso visual y no carga los estilos de Validaciones.
- No se modificaron Nginx, las redirecciones, la indexación ni los servicios de producto. No se purgó la caché global de Cloudflare.

**Verificación pública:** 205 comprobaciones de Validaciones, 148 de AI Notes y 18 adicionales de home/Validaciones en `www` escritorio/móvil. Los 18 archivos se comprobaron por HTTP y SHA-256 desde origen y ambos dominios, con igualdad tras normalizar sólo la protección de correo de Cloudflare en HTML. Los manifiestos coinciden exactamente. Ambas raíces siguen redirigiendo a V5; `/v4/` conserva su redirección previa. Nginx mantiene su hash de preflight y el release anterior se conserva. [Evidencia, herramientas y capturas](../features/validaciones/README.md#publicación--2026-09-12-utc).

**Continuidad:** aprobación y decisiones en [DESIGN](../../v5/DESIGN.md), estado en [PLAN_FEATURES](../../v5/PLAN_FEATURES.md) y [TODO](../../v5/TODO.md), escenas y fuentes en [brief y procedencia](../features/validaciones/README.md). El commit de documentación posterior registra el release real sin cambiar sus archivos. Los commits se pushean a la rama existente; no se integra el PR a `main` en esta publicación estática.

### Revertir sólo Validaciones

Confirmar que el enlace siga apuntando al release de esta publicación y reemplazarlo atómicamente por el anterior:

```sh
sudo python3 - <<'PYCODE'
from pathlib import Path
import os
link = Path('/var/www/alvia.ar/www/v5')
previous = Path('/var/www/alvia.ar/releases/v5-a4ca4aa')
assert link.is_symlink() and link.resolve() == Path('/var/www/alvia.ar/releases/v5-60dadfc')
assert previous.is_dir()
temporary = link.with_name('v5.rollback-60dadfc')
os.symlink(previous, temporary)
os.replace(temporary, link)
PYCODE
```

Después comprobar home y `release.json` en ambos dominios; la home anterior sólo enlaza AI Notes. No borrar releases, restaurar Nginx ni cambiar las redirecciones. El registro de una reversión posterior debe indicar qué commit queda servido.


## Correcciones de copy de Validaciones — 2026-09-12 UTC

Pablo corrigió el ejemplo a tres dígitos (`482`), pidió quitar las aclaraciones «En Alvia se llama…» y sobre número de afiliado/código de acceso, y sustituir la aclaración de segundo cobro por «Con todos los medios de pago». Se actualizó el fixture y la documentación editorial para conservar sus decisiones.

- Runtime: **`2d3380dc31ea832366bdd9a1fd4c5e50722b9d74`**, release `/var/www/alvia.ar/releases/v5-2d3380d`, activado atómicamente el **2026-09-12 18:12:57 UTC**.
- Paquete desde commit: 18 archivos, 472.759 bytes, más manifiesto; idéntico a la preview verificada. Único archivo de runtime modificado respecto del release anterior: `validaciones.html`.
- Validación: 205 comprobaciones locales y 205 públicas; HTTP/hashes del manifiesto y los 18 archivos en origen y ambos dominios. Home, AI Notes, estilos y scripts conservan sus hashes. No se modificaron Nginx ni rutas. [Evidencia actual](../features/validaciones/README.md#correcciones-de-copy--2026-09-12-utc).
- Release anterior preservado: `v5-60dadfc`. Para revertir, comprobar que el symlink aún apunta a `v5-2d3380d`, crear un enlace temporal al release `v5-60dadfc` y renombrarlo atómicamente sobre `/var/www/alvia.ar/www/v5`; verificar el manifiesto público. No usar el rollback de la primera publicación, que apunta a AI Notes.

Correcciones y evidencia commiteadas y pusheadas a `feat/v5-feature-pages-plan`. El commit posterior de documentación no cambia el runtime servido.


## Recetas — 2026-09-12 UTC

Pablo pidió publicar Recetas, commitear la implementación, documentar la publicación, volver a commitear y pushear. Se publicó el recorrido médico → paciente de la entrega local: preparación de receta y documento en el celular, selector nativo de PDF en preparación/listo, condición de demostración y enlaces recíprocos con Validaciones y AI Notes.

- Página: <https://alvia.ar/v5/recetas.html>, también en <https://www.alvia.ar/v5/recetas.html>.
- Runtime: **`5f74ecf7390da9f3e8d6f77b8b117ec87bdf8d95`**. Repositorio `Raboys/alvia-web`, rama `feat/v5-feature-pages-plan`.
- Release: `/var/www/alvia.ar/releases/v5-5f74ecf`. Symlink público `/var/www/alvia.ar/www/v5`, reemplazado atómicamente a las **18:44:43 UTC**.
- Release anterior conservado: `/var/www/alvia.ar/releases/v5-2d3380d`.
- Paquete desde commit: **20 archivos, 493.421 bytes**, más `release.json`. Coincide con la preview archivo por archivo; 232 comprobaciones del paquete servido por HTTP antes del cambio público.
- Las tres features están conectadas desde home y entre sí. Home incorpora sólo el enlace a Recetas; cada subpágina conserva sus propios estilos y scripts.
- No se modificaron Nginx, las redirecciones, el backend, la app, las consolas ni la indexación. No hubo reinicio de servicios o purga global de Cloudflare.

**Verificación pública:** 232 comprobaciones de Recetas en apex y 232 en www, 205 de Validaciones y 148 de AI Notes. Incluye cinco anchos de 320 a 1440 px, selector, teclado, sin JavaScript, movimiento reducido, texto ampliado y navegación conjunta. Los 20 archivos y manifiestos coinciden en origen y ambos dominios; el HTML sólo incorpora la protección conocida de correo de Cloudflare, normalizada antes de comparar. CSS/JS comprobados con sus URLs versionadas. Las raíces mantienen 301 a V5 y `/v4/` mantiene su redirección previa. Hash Nginx idéntico al preflight.

**Evidencia y continuidad:** [entrega publicada](../features/recetas/README.md#publicación--2026-09-12-utc), [manifiesto](../features/recetas/public/release.json), [HTTP](../features/recetas/public/http-checks.json), [cambio atómico](../features/recetas/public/deployment.json), [preflight](../features/recetas/public/before-deploy.json). DESIGN, PLAN_FEATURES, TODO y READMEs actualizados. La implementación `5f74ecf` se pusheó antes de publicar; un commit posterior registra documentación y evidencias sin cambiar el runtime. La publicación estática no integra la rama a `main`.

### Revertir sólo Recetas

Confirmar que el enlace todavía apunta a este release, y sustituirlo atómicamente por el anterior:

```sh
sudo python3 - <<'PYCODE'
from pathlib import Path
import os
link = Path('/var/www/alvia.ar/www/v5')
previous = Path('/var/www/alvia.ar/releases/v5-2d3380d')
assert link.is_symlink() and link.resolve() == Path('/var/www/alvia.ar/releases/v5-5f74ecf')
assert previous.is_dir()
temporary = link.with_name('v5.rollback-5f74ecf')
os.symlink(previous, temporary)
os.replace(temporary, link)
PYCODE
```

Después comprobar home y `release.json` en ambos dominios. El release anterior enlaza Validaciones y AI Notes y conserva Recetas informativa. No borrar releases, restaurar Nginx ni cambiar las redirecciones; registrar el commit que quede servido si se ejecuta la reversión.


## Tarjetas de funcionalidades · alternativa C — 2026-09-12 UTC

Pablo eligió C entre cuatro mocks y pidió aplicar, documentar, commitear, pushear y publicar. Home reemplaza el título subrayado con flecha diagonal por una tarjeta completa clickeable: borde suave, ilustración, título, descripción y «Explorar [función] →» al pie. Hay un solo enlace nativo por tarjeta, foco visible, navegación sin JS y movimiento reducido. Se conservan los textos, las ilustraciones y los destinos de las tres funciones.

- URL: <https://alvia.ar/v5/#funcionalidades>, también en `www.alvia.ar`.
- Runtime: **`f281d8b7d86ca3282967422684884bce554774df`**, commiteado y pusheado a `feat/v5-feature-pages-plan`.
- Release: `/var/www/alvia.ar/releases/v5-f281d8b`, activado atómicamente el **2026-09-12 19:04:33 UTC**.
- Release anterior conservado: `/var/www/alvia.ar/releases/v5-5f74ecf`.
- Paquete desde commit: 20 archivos, 494.924 bytes, más manifiesto. Igual a la preview validada. Sólo cambian `index.html` y `features.css`; el HTML exterior al bloque y las subpáginas conservan sus contenidos.
- Verificación: **53 comprobaciones locales, 53 públicas y 53 en www**; cinco anchos de 320 a 1440 px, teclado, clic en ilustraciones, sin JS, movimiento reducido, texto ampliado, nombres accesibles y ausencia de errores/desborde.
- HTTP y SHA-256 de los 20 archivos y manifiesto correctos desde origen y ambos dominios. CSS comprobado con la query `v=20260912-cards` usada por la home. Nginx, rutas de portada y versiones retiradas preservados.

[Decisión, capturas y resultados](../design/v5-feature-cards/README.md). La documentación y evidencia posteriores se commitean y pushean aparte; `release.json` sigue identificando el commit de runtime. Se continúa la rama institucional existente, sin integrar su PR a `main`.

### Revertir las tarjetas C

Confirmar que `/var/www/alvia.ar/www/v5` siga apuntando a `releases/v5-f281d8b`; crear un enlace temporal hacia `releases/v5-5f74ecf` y renombrarlo atómicamente sobre `www/v5`. Comprobar la home y el manifiesto en ambos dominios. El release anterior mantiene las tres funciones y sus páginas, con el acceso previo desde el título. No eliminar releases ni modificar la configuración de Nginx.


## Recetas · alternativa 3 sin documento intermedio

Pablo aprobó la alternativa 3 de los mocks y pidió aplicarla con sólo la vista médica
y la app del paciente. Aclaró expresamente que el header, textos y resto de la página
debían conservarse. Se reemplazan únicamente las dos superficies visuales y su presentación.

- Runtime: `59f16eb5c5b820484f4c3961b59fc026a2e5003e`, commiteado y pusheado a la rama institucional existente.
- Release: `/var/www/alvia.ar/releases/v5-59f16eb`, activado atómicamente a las **19:32:49 UTC**.
- Release anterior conservado: `/var/www/alvia.ar/releases/v5-f281d8b`.
- Paquete: 22 archivos, 506.431 bytes, más manifiesto; coincide con la preview validada.
- Sólo cambia `recetas.html` entre los archivos existentes; se agregan `recetas-mocks.css` y `recetas.js`. Los otros 19 archivos mantienen sus hashes.
- HTML fuera de los dos mocks idéntico, excepto referencias a los assets acotados al bloque.
- Verificación local: 235 comprobaciones; HTTP y hashes de los 22 archivos y manifiesto coinciden en origen y ambos dominios. Nginx y redirecciones se conservan.

[Capturas, decisión, verificaciones y rollback](../design/v5-prescription-mocks/README.md).
Para revertir esta iteración, verificar el destino actual del enlace y cambiarlo
atómicamente de `v5-59f16eb` a `v5-f281d8b`; no modificar configuración ni borrar releases.


### Recetas · documento generado sin comparador

Corrección inmediata de Pablo: quitar «Compará los estados del ejemplo» y sus
botones; la app debe mostrar sólo la receta generada. Se eliminan el selector,
el estado pendiente y el script de simulación, y se alinean las dos vistas.
El resto de la página mantiene su contenido y presentación.

- Runtime `0cd831deaccff40e848423106d4b98cdc4b480bf`, commiteado y pusheado.
- Release `/var/www/alvia.ar/releases/v5-0cd831d`, activado atómicamente a las **19:36:49 UTC**.
- 21 archivos, 503.554 bytes; runtime idéntico al verificado localmente.
- 174 comprobaciones locales y verificación pública de escritorio/móvil en ambos dominios.
- HTTP, hashes y manifiestos correctos en origen y ambos dominios; Nginx y rutas preservados.
- Release inmediatamente anterior: `v5-59f16eb`. Para volver al diseño original
  anterior a esta iteración, usar `v5-f281d8b`, que también se conserva.

[Estado final, capturas y resultados](../design/v5-prescription-mocks/README.md).


## Header · opción 4 Nombre + beneficio — 2026-09-12 UTC

Pablo eligió la alternativa 4 de los mocks, priorizó la descubribilidad en mobile y pidió documentar, commitear, pushear y publicar. Se reemplazan los enlaces a secciones de la home por Validaciones, AI Notes y Recetas, cada una con su beneficio. En mobile se ven tres tarjetas sin abrir un menú. Las cuatro páginas comparten header fijo, estado activo, Ingresar y Hablemos; la navegación usa HTML nativo y se retira el script del menú anterior.

- Runtime: **`6176676571a1366c9962800bbbf89295b2d60b97`**, commiteado y pusheado a `feat/v5-descriptive-header`.
- [PR #25](https://github.com/Raboys/alvia-web/pull/25), borrador con base main. Parte de la base de features ya publicada de #22; no se integran PR como parte del despliegue estático.
- Release: `/var/www/alvia.ar/releases/v5-6176676`, activado mediante cambio atómico de `/var/www/alvia.ar/www/v5` a las **19:58:24 UTC**.
- Release anterior conservado: `/var/www/alvia.ar/releases/v5-0cd831d`.
- Paquete desde commit: **21 archivos, 505.808 bytes**, más manifiesto. Todos sus hashes coinciden con la preview elegida.
- Verificación: 36 combinaciones de página/ancho en preview y 36 en cada dominio público, de 320 a 1440 px; enlaces visibles con área táctil mínima de 44 px, estado activo, navegación entre features, header fijo y funcionamiento sin JavaScript. Sin errores HTTP/JS ni desbordes.
- HTTP y SHA-256 de archivos y manifiestos correctos en origen, apex y www; normalización limitada a la protección de correo de Cloudflare. Nginx, redirecciones y servicios preservados.
- Los cuatro mocks, la elección y las capturas se conservan en `previews/header-variants/` dentro de `Raboys/alvia-web`; no se incluyen en el runtime público.

[Decisión, verificación reproducible, capturas y rollback](../design/v5-header-option4/README.md). La evidencia y documentación posterior se commitean y pushean aparte; el manifiesto público continúa identificando el commit de runtime `6176676`.

### Revertir el header opción 4

Confirmar que el enlace público todavía apunte a `releases/v5-6176676`; crear un enlace temporal a `releases/v5-0cd831d` y reemplazar `www/v5` mediante rename atómico. Verificar home, subpáginas y manifiesto en ambos dominios. No modificar redirecciones, restaurar Nginx ni eliminar releases.

## Chevrones B · 2026-09-12 UTC

Pablo eligió B y autorizó commit, push, documentación y publicación. Runtime `8276775`, 30 chevrones en cuatro páginas y retirada de una diagonal decorativa. El release conserva el contenido y header publicados en `6176676`; sólo cambian cuatro HTML y el CSS compartido. [Registro completo y rollback](2026-09-12-chevron-b.md).
