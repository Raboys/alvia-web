# V4 — historial de publicación

Estado vigente: portada en `https://alvia.ar/` → `/v4/`, con recorrido automático publicado desde `d07ab14`. V3 sigue disponible en `/v3/`. Los bloques siguientes registran cada etapa en orden cronológico.

## Alcance autorizado

Pablo pidió publicar V4-C en `https://alvia.ar/v4/`, compartirla para revisión, documentar todos los avances y hacer merge a `main`. La decisión se tomó el 4 de septiembre de 2026, hora local; el registro y el despliegue usan fecha UTC del 5 de septiembre.

- Fuente: `v4-c/` de `Raboys/alvia-web`.
- V3 continúa siendo la portada (`/` redirige a `/v3/`).
- A y B se integran a Git como antecedentes; sólo C se publica en `/v4/`.
- Se conserva `noindex, nofollow`; es una URL pública para revisión, no un sitio privado.

## Avances incluidos

1. **Lenguaje simple.** «Tus médicos, atendiendo online», el problema de las horas médicas desaprovechadas y explicación breve de cómo aprovechar la red propia.
2. **Coberturas.** Identidad, autorización y copago según las reglas de cada cobertura, junto a la demo.
3. **App consistente.** Inicio, identidad, autorización, copago y receta abierta usan el mismo teléfono, proporción, hora, señal, batería y barra inferior.
4. **Demos en movimiento.** Identidad automática en bucle; código ingresado dígito por dígito, confirmación y reinicio. Se pausan cuando dejan de estar visibles.
5. **Carga anticipada.** Imágenes WebP y videos pequeños, preload y decodificación; cambiar de pestaña no inicia nuevas descargas.
6. **Explorador rediseñado.** Iconos, selección clara, navegación que acompaña el desplazamiento en móvil y ampliación en la misma página.
7. **Receta del afiliado.** Documento abierto con indicación, profesional y acciones; sustituye la lista de recetas.
8. **Herramientas del médico.** Demos aisladas de generación de recetas y lectura/ampliación de estudios, lado a lado en escritorio y apiladas en móvil.
9. **AI Notes.** Sección propia con resumen simple y botón Copiar funcional; sin «SOAP», estados internos ni aclaraciones técnicas en el mock.
10. **Publicación autocontenida.** Agenda local a V4, metadata para compartir y paquete reproducible sin documentación, generadores ni assets históricos sin uso.

Los datos son ficticios. Las pantallas del afiliado se renderizan desde componentes reales; las tarjetas del médico y AI Notes son ejemplos HTML/CSS simplificados. No se emiten recetas, procesan pagos, reciben estudios ni envían verificaciones desde la landing.

## Evidencia previa

- Recorrido de app, video y carga: `/tmp/alvia-v4c-final-review/`.
- Diseño responsive y ampliación: `/tmp/alvia-v4c-tour-design/`.
- Receta abierta, cobertura y contenido: `/tmp/alvia-v4c-content-review/`.
- Copiar AI Notes y legibilidad: `/tmp/alvia-notes-simple/`.
- Receta generada y visor de estudios: `/tmp/alvia-doctor-features/`.
- Historia detallada y procedencia: [README de V4-C](../../v4-c/README.md).

Las pruebas usan Chrome a 1440, 1024, 768, 390 y 320 px, según cada cambio, incluyendo alternativas sin JavaScript. El script de empaquetado produce un manifiesto de hashes para verificar que se sirve el código integrado.

## Primera publicación

Estado: **publicada y verificada** en <https://alvia.ar/v4/>.

- PR de integración: [#13](https://github.com/Raboys/alvia-web/pull/13), merge el 2026-09-05 a las 04:29:34 UTC.
- Commit de la primera publicación: `67c115f903b13e7af8b9d3843dcd5138c56cb867`.
- Directorio de release: `/var/www/alvia.ar/releases/v4-67c115f`.
- Enlace de publicación: `/var/www/alvia.ar/www/v4` → ese release al publicarse por primera vez; destino vigente indicado abajo.
- Manifiesto público: <https://alvia.ar/v4/release.json>.
- Paquete: 19 archivos de runtime, 2.165.366 bytes, más el manifiesto. Incluye originales para ampliación y la imagen para compartir; no es el peso de descarga inicial.

### Comprobación pública

- `/v4` devuelve 301 a `https://alvia.ar/v4/`; `/v4/` y `www.alvia.ar/v4/` responden 200.
- `/` conserva su 301 a `https://alvia.ar/v3/`.
- Los 19 archivos servidos directamente por Nginx coinciden byte a byte con el manifiesto del commit desplegado. En Cloudflare, 18 coinciden directamente; el HTML coincide tras normalizar únicamente su transformación automática del correo de contacto, verificada por separado.
- Manifiesto público idéntico al generado desde Git. V1, V2, V3 y la configuración Nginx conservan sus hashes previos.
- Chrome sobre la URL pública: 1440, 390 y 320 px, más 390 sin JavaScript. Galería, videos automáticos, receta abierta, ampliación, generación de receta de ejemplo, cambio/lectura de estudios, Copiar AI Notes, FAQ y anclas correctos; sin errores de recursos ni desborde horizontal.
- Evidencia pública: `/tmp/alvia-v4-public-review/` y `/tmp/alvia-v4-public-verification.json`.
- La comparación V3/V4 posterior detectó dos mensajes demasiado implícitos: gestión de horarios/reservas por el equipo de la organización y continuidad de la historia clínica (paciente, profesional, fecha). Pablo aprobó sumarlos junto al beneficio de reducir la presión sobre la guardia presencial. Se incorporan en los bloques de agenda y atención médica; el detalle del texto queda en el README de V4-C.

El repositorio institucional no tiene CI de despliegue. Se publica desde un commit de Git en `/var/www/alvia.ar/releases/v4-<commit>/`, con un enlace atómico `/var/www/alvia.ar/www/v4` a ese release. Nginx ya sirve el prefijo; no requiere cambios ni recarga.

## Actualización de comunicación — 2026-09-05 UTC

Estado: **publicada y verificada** en <https://alvia.ar/v4/>.

- PR: [#15](https://github.com/Raboys/alvia-web/pull/15), integrado a `main` a las 04:39:43 UTC.
- Commit desplegado: `53dfc0c1c64ff63b58041740f1c716e090442d36`.
- Enlace activo: `/var/www/alvia.ar/www/v4` → `/var/www/alvia.ar/releases/v4-53dfc0c`.
- Release anterior conservado: `/var/www/alvia.ar/releases/v4-67c115f`.
- Tres mensajes incorporados: horarios y reservas visibles para el equipo, registro de cada atención con paciente/profesional/fecha y más consultas dentro de la red con menos presión sobre la guardia presencial.
- Paquete: 19 archivos de runtime, 2.165.511 bytes, más el manifiesto; sólo cambia el HTML respecto del release anterior.
- Chrome en la URL pública a 1440, 390 y 320 px: tres frases presentes, sin desborde horizontal ni errores de recursos o JavaScript. Capturas: `/tmp/alvia-v4-copy-public-review/`.
- Los 19 archivos coinciden con el manifiesto en Nginx y Cloudflare, normalizando en el HTML únicamente la ofuscación automática del correo. Manifiesto público idéntico al generado desde el commit. Evidencia: `/tmp/alvia-v4-copy-public-verification.json`.
- `/v4` conserva su 301 a `/v4/`; `www.alvia.ar/v4/` responde 200; `/` sigue redirigiendo a `/v3/`. Los hashes de V1, V2, V3 y Nginx siguen intactos.

## Rollback

Para revertir la actualización de comunicación, comprobar que el enlace activo apunta a `v4-53dfc0c`, crear un enlace temporal a `/var/www/alvia.ar/releases/v4-67c115f` y renombrarlo sobre `/var/www/alvia.ar/www/v4` con `sudo mv -T`.

En esta primera publicación no existía `/v4`. Para retirarla, comprobar el destino del enlace con `readlink /var/www/alvia.ar/www/v4` y ejecutar `sudo unlink /var/www/alvia.ar/www/v4`. El release queda conservado y `/v3/` sigue disponible.

En actualizaciones posteriores se conserva el release anterior y se intercambia el enlace por uno que apunte a ese directorio. No se borra ni sincroniza el document root completo.

## V4 como portada — 2026-09-05 UTC

Por pedido de Pablo, la raíz de `alvia.ar` y `www.alvia.ar` ahora redirige con 301 a `https://alvia.ar/v4/`. V3 sigue accesible mediante `/v3/` y `/v3` redirige a su variante con barra final.

Cambio operativo: en `/etc/nginx/sites-available/alvia.ar`, únicamente el destino de `location = /` del servidor institucional pasa de V3 a V4, junto con sus comentarios descriptivos. Se comprobó la configuración con `sudo nginx -t` y se aplicó con `sudo systemctl reload nginx`.

Verificación: 16 comprobaciones de rutas entre apex/www y origen/Cloudflare; portada a V4, V3 directa y V4 respondiendo 200. El contenido de V3, V4 y el manifiesto permanecieron idénticos durante este cambio. Evidencia: `/tmp/alvia-home-v4/verification.json`.

Backup de configuración: `/etc/nginx/sites-available/alvia.ar.before-home-v4-20260905`. Para revertir sólo esta decisión, cambiar el destino de la raíz a `https://alvia.ar/v3/`, comprobar con `nginx -t` y recargar. Evitar restaurar el archivo completo si hubo cambios posteriores en otras rutas.

## Recorrido automático publicado — 2026-09-05 UTC

- Implementación: [PR #17](https://github.com/Raboys/alvia-web/pull/17), merge `359cb924d63e86c002b8ec97a284f4464cbb78a5`.
- Corrección de caché: [PR #18](https://github.com/Raboys/alvia-web/pull/18), merge y **fuente desplegada vigente** `d07ab14a1d8b5ffc3d2c7ebf5826c852b2e27101`.
- Enlace activo: `/var/www/alvia.ar/www/v4` → `/var/www/alvia.ar/releases/v4-d07ab14`.
- Paquete: 19 archivos de runtime, 2.169.920 bytes, más manifiesto. No se agregan archivos multimedia ni dependencias.
- Inicio → Identidad → Autorización → Copago → Recetas → Inicio, cada siete segundos. Barra decreciente y transición de opacidad/desplazamiento; la selección manual detiene el recorrido hasta pulsar «Reproducir recorrido».
- Verificación local en Chrome a 1440, 390 y 320 px: ciclo completo, progreso, videos, selección manual detenida más de siete segundos, reanudación, ampliación, sin nuevas descargas ni errores/desborde. Movimiento reducido y navegación sin JavaScript comprobados. Evidencia: `/tmp/alvia-v4-autoplay-review/`.
- La primera comprobación pública detectó JS/CSS antiguos en Cloudflare. Se versionaron sus URLs como `20260905-tour-autoplay`. La nueva comprobación desde `https://alvia.ar/` verificó la redirección, el avance automático y la detención al seleccionar manualmente, sin errores ni desborde en móvil. Evidencia: `/tmp/alvia-v4-autoplay-public-review/`.
- Los 19 archivos coinciden con el manifiesto en origen y Cloudflare usando las URLs efectivas del HTML (incluyendo versiones); sólo se normaliza la ofuscación automática del correo en el HTML. V1/V2/V3 mantienen sus hashes y Nginx coincide con el cambio acotado de portada. Evidencia: `/tmp/alvia-v4-autoplay-public-verification.json`.

Para volver al recorrido manual anterior, intercambiar atómicamente el enlace V4 por `/var/www/alvia.ar/releases/v4-53dfc0c`, conservado. La raíz seguirá apuntando a `/v4/`; el rollback del destino de portada se describe por separado arriba.
